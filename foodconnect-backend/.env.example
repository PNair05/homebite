# Copy this file to .env and fill in values. Settings use the FC_ prefix.

# FastAPI app settings
FC_APP_NAME="FoodConnect Backend"
FC_DEBUG=true

# SQLite (default)
# FC_DATABASE_URL="sqlite:///./app.db"

# Postgres via TCP (psycopg3 driver) - replace with your values
# FC_DATABASE_URL="postgresql+psycopg://<USER>:<PASSWORD>@<HOST>:<PORT>/<DBNAME>"

# AWS RDS (recommended) - use your RDS endpoint
# FC_DATABASE_URL="postgresql+psycopg://<USER>:<ENCODED_PASS>@<RDS_ENDPOINT>:5432/<DBNAME>"

# Optional API keys
# FC_OPENAI_API_KEY=""

# If you later adopt RDS IAM auth, you'll generate a token per-connection in code and set sslmode=require.
# See README for details.
